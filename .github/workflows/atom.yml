name: Security Code Review

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    name: "Test ABC"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Success
        run: echo "Success"
