name: My Test

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  test1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Success
        run: echo "Success"
